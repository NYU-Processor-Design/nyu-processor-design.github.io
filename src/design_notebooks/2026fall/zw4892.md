# Ray Wang's Design Notebook

## Week 1

This week I completed Lab 1. I installed Git and Verilator with Homebrew on macOS, created a public onboarding repository, wrote a two-input AND gate in SystemVerilog plus a testbench, and ran the simulation with Verilator until all four cases passed. I also went through the full GitHub workflow: branch, commit, push, open a pull request, and squash-merge into `main`.

I learned how Verilator turns SystemVerilog into a runnable simulation, and why generated files like `obj_dir/` belong in `.gitignore`. The `logic` type and the testbench `#1` delays still feel new, but the AND-gate example made the basic RTL-plus-testbench flow clearer.

The main problem was GitHub setup. I first created the repositories on the wrong account, then had to move everything to `zw4892-ray`. I finished authentication in Safari and added a separate SSH key for this account.

I am especially interested in Bus/SoC and UART+SPI, and I hope to spend more time learning these areas this semester.

### Work

- [Processor Design Onboarding](https://github.com/zw4892-ray/processor-design-onboarding)
